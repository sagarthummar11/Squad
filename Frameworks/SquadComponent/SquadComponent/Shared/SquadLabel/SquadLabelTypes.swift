//
//  SquadLabelTypes.swift
//  SquadComponent
//
//  Created by Sagar Thummar on 13/04/2022.
//

import UIKit
import SquadStyle


public class Title1Label: SquadLabel {
    
    override func setup() {
        super.setup()
        
        font = SquadFont.title1.font
        textColor = SquadColor.blackOlive.color
    }
}

public class Title2Label: SquadLabel {
    
    override func setup() {
        super.setup()
        
        font = SquadFont.title2.font
        textColor = SquadColor.blackOlive.color
    }
}

public class Title3Label: SquadLabel {
    
    override func setup() {
        super.setup()
        
        font = SquadFont.title3.font
        textColor = SquadColor.blackOlive.color
    }
}

public class Headline1Label: SquadLabel {
    
    override func setup() {
        super.setup()
        
        font = SquadFont.headline1.font
        textColor = SquadColor.blackOlive.color
    }
}

public class Headline2Label: SquadLabel {
    
    override func setup() {
        super.setup()
        
        font = SquadFont.headline2.font
        textColor = SquadColor.blackOlive.color
    }
}

public class SmallHeadingLabel: SquadLabel {
    
    override func setup() {
        super.setup()
        
        font = SquadFont.smallHeading.font
        textColor = SquadColor.blackOlive.color
    }
}

public class BodyLargeLabel: SquadLabel {
    
    override func setup() {
        super.setup()
        
        font = SquadFont.bodyLarge.font
        textColor = SquadColor.blackOlive.color
    }
}

public class BodyBoldLabel: SquadLabel {
    
    override func setup() {
        super.setup()
        
        font = SquadFont.bodyBold.font
        textColor = SquadColor.blackOlive.color
    }
}

public class BodyLabel: SquadLabel {
    
    override func setup() {
        super.setup()
        
        font = SquadFont.body.font
        textColor = SquadColor.blackOlive.color
    }
}

public class BodySmallLabel: SquadLabel {
    
    override func setup() {
        super.setup()
        
        font = SquadFont.bodySmall.font
        textColor = SquadColor.blackOlive.color
    }
}

public class SubHeadLabel: SquadLabel {
    
    override func setup() {
        super.setup()
        
        font = SquadFont.subhead.font
        textColor = SquadColor.blackOlive.color
    }
}

public class FooterLabel: SquadLabel {
    
    override func setup() {
        super.setup()
        
        font = SquadFont.footnote.font
        textColor = SquadColor.blackOlive.color
    }
}
